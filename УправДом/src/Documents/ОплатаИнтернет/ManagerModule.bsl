
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда



#Область ПрограммныйИнтерфейс


// Переопределяет настройки печати для объекта.
    //
    // Параметры:
    //  Настройки - см. УправлениеПечатью.НастройкиПечатиОбъекта.
    //
Процедура ПриОпределенииНастроекПечати(Настройки) Экспорт
 	
	Настройки.ПриДобавленииКомандПечати = Истина;
           
КонецПроцедуры


 // Заполняет список команд печати.
    // 
    // Параметры:
    //   КомандыПечати - см. УправлениеПечатью.СоздатьКоллекциюКомандПечати
    //
Процедура ДобавитьКомандыПечати(КомандыПечати) Экспорт
        
		Команда = КомандыПечати.Добавить();		
//		Команда.МенеджерПечати = "Документ.ОплатаГазоснабжения"; //Можно не указывать т.к. Мен-р сам документ.
		Команда.Идентификатор = "ПечатьИнтернет";
		Команда.Представление = НСтр("ru = 'Печать платежа'");
		Команда.ПроверкаПроведенияПередПечатью = Истина;
		Команда.Порядок = 10;  
		
          
КонецПроцедуры


// Формирует печатные формы.
//
// Параметры:
// Массив - МассивОбъектов - ссылки на объекты, которые нужно распечатать;
// Структура - ПараметрыПечати  - дополнительные настройки печати;
// ТаблицаЗначений - КоллекцияПечатныхФорм - сформированные табличные документы (выходной параметр)
// СписокЗначений - ОбъектыПечати - значение - ссылка на объект;
//                                            представление - имя области, в которой был выведен объект (выходной параметр);
// Структура - ПараметрыВывода - дополнительные параметры сформированных табличных документов (выходной параметр).
//
Процедура Печать(МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода) Экспорт
	
	ПечатнаяФорма = УправлениеПечатью.СведенияОПечатнойФорме(КоллекцияПечатныхФорм, "ПечатьИнтернет");
	Если ПечатнаяФорма <> Неопределено Тогда
	    ПечатнаяФорма.ТабличныйДокумент = ПечатьИнтернет(МассивОбъектов, ОбъектыПечати);
	    ПечатнаяФорма.СинонимМакета = НСтр("ru = 'Печать оплаты интернет'");
	    ПечатнаяФорма.ПолныйПутьКМакету = "Документ.ОплатаИнтернет.ПФ_MXL_ПечатьИнтернет";	    
	КонецЕсли;	
	
КонецПроцедуры


#КонецОбласти


#Область СлужебныеПроцедурыИФункции

// Процедура заполняет табличный документ для печати.
//
// Параметры:
//	Массив - МассивОбъектов ,  ссылки на объекты, которые нужно распечатать;
//  СписокЗначений - ОбъектыПечати ,  значение  ссылка на объект;
//  представление  имя области, в которой был выведен объект (выходной параметр);
//  
// Возвращаемое значение:
// ТабличныйДокумент - ТабличныйДокумент
Функция ПечатьИнтернет(МассивОбъектов, ОбъектыПечати) Экспорт
	
	ТабличныйДокумент = Новый ТабличныйДокумент();
	ТабличныйДокумент.КлючПараметровПечати = "ПараметрыПечати_ПечатьИнтернет";
		
	Макет = УправлениеПечатью.МакетПечатнойФормы("Документ.ОплатаИнтернет.ПФ_MXL_ПечатьИнтернет");
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ОплатаИнтернет.Ссылка КАК Ссылка,
		|	ОплатаИнтернет.Номер КАК Номер,
		|	ОплатаИнтернет.Дата КАК Дата,
		|	ОплатаИнтернет.ВидПлатежа КАК ВидПлатежа,
		|	ОплатаИнтернет.ОплачиваемыйПериодСлужебный КАК ПериодОплСлужебный,
		|	ОплатаИнтернет.Поставщик КАК Поставщик,
		|	ОплатаИнтернет.Поставщик.ИНН КАК ПоставщикИНН,
		|	ОплатаИнтернет.Поставщик.Телефон КАК ПоставщикТелефон,
		|	ОплатаИнтернет.Поставщик.ЭлектроннаяПочта КАК ПоставщикЭлектроннаяПочта,
		|	ОплатаИнтернет.Поставщик.Сайт КАК ПоставщикСайт,
		|	ОплатаИнтернет.Поставщик.ЛицевойСчет КАК ПоставщикЛицевойСчет,
		|	ОплатаИнтернет.Комментарий КАК Комментарий,
		|	ОплатаИнтернет.УчитыватьДопУслугиИнтернет КАК УчитыватьДопыИнтернет,
		|	ОплатаИнтернет.ДанныеПлатежа.(
		|		СуммаПлатежа КАК СуммаПлатежа,
		|		ТарифИнтернет КАК ТарифИнтернет,
		|		ТарифДопУслугиИнтернет КАК ТарифДопУслугиИнтернет) КАК ДанныеПлатежа
		|ИЗ
		|	Документ.ОплатаИнтернет КАК ОплатаИнтернет
		|ГДЕ
		|	ОплатаИнтернет.Ссылка В (&Ссылка)";
	Запрос.Параметры.Вставить("Ссылка", МассивОбъектов);
	
	РезультатЗапроса = Запрос.Выполнить();

	РаботаСДокументамиПечатьСерверУД.ВыводНаПечатьОплаты(ТабличныйДокумент, Макет, РезультатЗапроса, ОбъектыПечати);
	
	Возврат ТабличныйДокумент;
	
	
КонецФункции


#КонецОбласти

#КонецЕсли
